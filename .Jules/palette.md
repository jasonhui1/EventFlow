## 2024-05-24 - Interactive Component Tooltips
**Learning:** `title` attributes are often insufficient for screen readers, especially in complex dynamic components like file-trees and tab bars.
**Action:** Always complement `title` attributes on icon-only buttons with explicit, descriptive `aria-label`s (using dynamic data like item names where possible).
